<div class="text-center mb-4">
  <small class="text-uppercase text-muted fw-semibold letter-spacing-wide">Strety</small>
  <h2 class="fw-bold mt-1 mb-4">Community Board</h2>
</div>

<hr class="mb-4">

<h5 class="mb-3 text-muted fw-semibold">My Bookmarks</h5>

<% if @bookmarks.any? %>
  <ul class="bookmark-list list-unstyled mb-2">
    <% @bookmarks.each do |bookmark| %>
      <li class="py-2 border-bottom">
        <% if bookmark.bookmarkable.present? %>
          <%= link_to bookmark.name, bookmark.bookmarkable, class: "text-decoration-none" %>
        <% else %>
          <span class="bookmark-tombstone text-muted text-decoration-line-through">
            <i class="bi bi-exclamation-circle me-1"></i><%= bookmark.name %>
          </span>
          <small class="text-muted d-block">This post has been deleted.</small>
        <% end %>
      </li>
    <% end %>
  </ul>
  <%= link_to "View all bookmarks →", bookmarks_path, class: "text-muted small" %>
<% else %>
  <p class="bookmark-list-empty text-muted fst-italic">No bookmarks yet. Bookmark a post to see it here.</p>
<% end %>

<hr class="my-4">

<h5 class="mb-3 text-muted fw-semibold">Recent Posts</h5>

<div class="row g-4">
  <% @recent_posts.each do |post| %>
    <div class="col-md-4">
      <div class="card h-100">
        <div class="card-body d-flex flex-column">
          <h5 class="card-title"><%= link_to post.title, post, class: "text-decoration-none" %></h5>
          <p class="card-text text-muted flex-grow-1"><%= truncate(post.body, length: 120) %></p>
          <div class="d-flex justify-content-between align-items-center mt-auto">
            <small class="text-muted"><%= time_ago_in_words(post.created_at) %> ago</small>
            <%= render "posts/bookmark_button", post: post %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
